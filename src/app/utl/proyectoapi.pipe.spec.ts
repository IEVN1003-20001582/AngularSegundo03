import { ProyectoapiPipe } from './proyectoapi.pipe';

describe('ProyectoapiPipe', () => {
  it('create an instance', () => {
    const pipe = new ProyectoapiPipe();
    expect(pipe).toBeTruthy();
  });
});
